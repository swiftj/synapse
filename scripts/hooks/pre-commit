#!/bin/bash
# Pre-commit hook: Code quality checks
#
# NOTE: Version bumping was moved to prepare-commit-msg because
# pre-commit runs BEFORE Git writes COMMIT_EDITMSG, so it cannot
# reliably read the current commit message.
#
# Install: Run ./scripts/install-hooks.sh
# Location: scripts/hooks/pre-commit

# Reserved for lint, format, or test checks
exit 0
